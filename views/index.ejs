<%- include("layout/header") %>

    <main>
        <form action="/" method="post">
            <input type="text" placeholder="programming language" name="pLangauge">
            <button type="submit">Add</button>
        </form>

        <ul>
            <% if(plNames.length> 0){ %>

                <% for(let X=0; X < plNames.length; X++){ %>
                    <li>
                        <%= plNames[X] %>
                    </li>
                    <% } %>

                        <% } else{ %>
                            <p>No data found</p>
                            <% } %>
        </ul>

    </main>

    <%- include("layout/footer") %>